import { styled } from '../../stitches.config';

export const Phrase = styled('q', {
  display: 'block',
  fontFamily: '$text',
  fontStyle: 'italic',
  margin: '1rem 0 2rem',
  fontSize: '1.25rem',
});
